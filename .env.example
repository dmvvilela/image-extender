# Optional server-side fallback keys for local dev or self-hosted demos.
# The app is BYOK by default — keys are collected in-browser on first run
# and stored in localStorage, never on the server.
#
# Google AI Studio: https://aistudio.google.com/apikey
# OpenAI:           https://platform.openai.com/api-keys

GOOGLE_GENERATIVE_AI_API_KEY=
OPENAI_API_KEY=
